# 個人用OneDrive 初回設定

この設定は、アプリからあなたの個人用OneDriveへ安全にサインインするために必要です。
クライアントシークレットや有料サーバーは不要です。

## 事前に用意するもの

- 作戦保存に使う個人用Microsoftアカウント
- GitHub Pagesで公開した作戦ボードのURL

公開URLの例：

```text
https://GitHubユーザー名.github.io/basketball-tactics-board/
```

## 1. Microsoft Entraでアプリを登録

1. `https://entra.microsoft.com/` を開き、Microsoftアカウントでサインインします。
2. 「ID」→「アプリケーション」→「アプリの登録」を開きます。
3. 「新規登録」を押します。
4. 名前を `ZEROONE CANVAS` にします。既存のアプリ登録は旧名称のままでも動作します。
5. サポートされているアカウントの種類は、個人用Microsoftアカウントを含む項目を選びます。
   - 個人だけで使う場合：「個人用Microsoftアカウントのみ」
   - その項目がない場合：「任意の組織ディレクトリ内のアカウントと個人用Microsoftアカウント」
6. リダイレクトURIの種類を「シングルページ アプリケーション (SPA)」にします。
7. URIへGitHub Pagesの公開URLを、末尾の `/` まで正確に入力します。
8. 「登録」を押します。

アプリ画面の「概要」に表示された「アプリケーション (クライアント) ID」をコピーします。

## 2. OneDriveの権限を追加

1. 登録したアプリの「APIのアクセス許可」を開きます。
2. 「アクセス許可の追加」→「Microsoft Graph」→「委任されたアクセス許可」を選びます。
3. `Files.ReadWrite.AppFolder` を検索して追加します。

この権限は、このアプリ専用のOneDriveフォルダだけを読み書きできます。
「クライアント シークレット」は作成しないでください。

## 3. クライアントIDを公開ファイルへ設定

ZIPを展開し、次のファイルをテキストエディターで開きます。

```text
1_App/js/onedrive-config.js
```

次の部分を、手順1でコピーしたクライアントIDへ置き換えます。

```javascript
clientId: "YOUR_MICROSOFT_ENTRA_CLIENT_ID",
```

例：

```javascript
clientId: "12345678-1234-1234-1234-123456789abc",
```

変更したファイルをGitHubへ上書きアップロードします。
GitHub上で直接編集する場合は、対象ファイルを開いて鉛筆ボタンを押し、変更をコミットしても構いません。

## 4. PCとiPadで接続

1. GitHub Pagesの公開URLを再読み込みします。
2. 上部の「OneDrive接続」を押します。
3. 「Microsoftアカウントで接続」を押します。
4. 保存に使う個人用Microsoftアカウントでサインインします。
5. アクセス許可の確認画面で同意します。

接続後、上部に「☁ OneDrive接続中」と表示されます。
各PCとiPadで同じMicrosoftアカウントへ一度ずつ接続してください。

## 保存場所

OneDrive内に次の場所が自動作成されます。

```text
アプリ / Basketball Tactics Board /（既存登録）
または
アプリ / ZEROONE CANVAS /（新規登録）
```

その中へ `Shared`、`U12`、`U15` などの分類フォルダと作戦JSONが保存されます。

## よくあるエラー

### リダイレクトURIが一致しない

Microsoft Entraへ登録したURIと、ブラウザーのアドレス欄のURLが完全に一致しているか確認します。
末尾の `/`、大文字小文字、リポジトリ名も一致させます。

### 接続ボタンが有効にならない

`onedrive-config.js` のクライアントIDが正しいか確認し、GitHub Pagesを再読み込みします。
アプリ内の「設定」→「詳細設定」→「クライアントIDを変更する」から、この端末だけの上書き値を設定して確認することもできます。通常の設定画面にはクライアントIDは表示されません。

### 別の端末に最新作戦が出ない

編集した端末で「作戦保存」を押してから、もう一方の端末の「ライブラリ」→「更新」を押します。
